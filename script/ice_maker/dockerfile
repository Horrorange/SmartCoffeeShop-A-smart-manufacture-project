# 使用 Debian slim 基础镜像，兼容 python-snap7 预编译wheel
FROM python:3.11-slim

# 设置工作目录为 /app
WORKDIR /app

# 复制依赖文件
COPY requirements.txt .

# 安装依赖
RUN pip install --no-cache-dir -r requirements.txt

# 复制所有文件
COPY . .

# 启动容器
CMD ["python", "icemaker_sim.py"]




